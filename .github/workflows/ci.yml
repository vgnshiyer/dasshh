name: CI

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.13"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        run: |
          curl -Ls https://astral.sh/uv/install.sh | sh
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Create virtual environment
        run: uv venv

      - name: Install dependencies
        run: |
          uv pip install -e ".[dev]"
          uv pip install build

      - name: Lint with ruff (if available)
        run: |
          uv pip install ruff || echo "Ruff not available, skipping linting"
          uv run ruff check . || echo "No ruff configuration found, skipping linting"
          uv run ruff format --check . || echo "No ruff configuration found, skipping linting"
        continue-on-error: true

      - name: Run tests with pytest
        run: |
          uv run pytest tests/ -v --cov=dasshh --cov-report=xml --cov-report=term-missing --cov-report=html

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          file: ./coverage.xml
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Upload HTML coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-html-report
          path: htmlcov/
          if-no-files-found: warn

      - name: Test build package
        run: |
          uv run python -m build

      - name: Test package installation
        run: |
          uv pip install dist/*.whl